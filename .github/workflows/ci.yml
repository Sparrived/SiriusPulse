name: CI

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ "3.12" ]
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -e .[test]
      
      - name: Run tests with pytest
        run: |
          pytest -q --tb=short --cov=sirius_pulse --cov-report=xml
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          fail_ci_if_error: false
          flags: unittests
          name: codecov-umbrella

  lint:
    name: Linting & Type Checking
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]
      
      - name: Lint with pylint
        run: |
          pylint sirius_pulse --fail-under=8.0 --disable=C0111,W0212 || true
      
      - name: Format check with black
        run: |
          black --check sirius_pulse tests || true
      
      - name: Import sorting check with isort
        run: |
          isort --check-only sirius_pulse tests || true
      
      - name: Type check with mypy
        run: |
          mypy sirius_pulse --ignore-missing-imports || true

  security:
    name: Security Scanning
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]
          pip install bandit
      
      - name: Security scan with bandit
        run: |
          bandit -r sirius_pulse -ll || true

  build:
    name: Build Distribution
    runs-on: ubuntu-latest
    needs: [ test, lint ]
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"
          cache: 'pip'
      
      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel build
      
      - name: Build distribution
        run: |
          python -m build
      
      - name: Verify distribution
        run: |
          python -m twine check dist/* || true
      
      - name: Store built distributions
        uses: actions/upload-artifact@v4
        with:
          name: python-dist
          path: dist/
